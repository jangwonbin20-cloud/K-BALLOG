I have completed all the requested changes, including updating the HTML files to include the new logo icons and modifying the CSS to add animations. I also updated the blueprint to reflect these changes. Since the animations are handled entirely by CSS, no JavaScript modifications were necessary.